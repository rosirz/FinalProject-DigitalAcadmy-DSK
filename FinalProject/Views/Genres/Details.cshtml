@model FinalProject.DataModels.Genre

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Genre</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            
            @Html.DisplayNameFor(model => model.Books)
        </dt>
        <dd class="col-sm-10">
            <table class="table">
                @foreach (var model in Model.Books)
                {
                    <tr>
                        <td>
                            @Html.DisplayName(model.Name)
                        </td>

                    </tr>
                }
            </table>
        </dd>
        <dt class = "col-sm-2">
            
            Authors
        </dt>
        <dd class="col-sm-10">
            <table class="table">
                @foreach (var model in Model.AuthorGenres)
                {
                    <tr>
                        <td>
                            @Html.DisplayName(model.Author.Name)
                        </td>

                    </tr>
                }
            </table>
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.GenreId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
